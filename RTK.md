# Reatom v1000 compatibility entrypoint

This file exists because local instructions still reference `@RTK.md`.

Do not duplicate or maintain a second handbook here.
Use the real skill entrypoints instead:

- `SKILL.md` for when and how to use the skill
- `references/reatomv1000.md` for the actual Reatom v1000 handbook

Start with `references/reatomv1000.md` and route by section:

- Quick start
- Async model and async context
- State modeling
- Lifecycle and events
- Forms
- Routing
- Persistence and tooling
- SSR, testing, Suspense, and migration
- Best practices and troubleshooting

If the handbook is insufficient, inspect only the relevant paths in `assets/reatom`.
