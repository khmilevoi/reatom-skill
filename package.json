{
  "name": "reatom-claude-plugin",
  "private": true,
  "description": "Development scripts and tests for the reatom Claude Code plugin. Not published; the plugin's own version lives in .claude-plugin/plugin.json and is deliberately not duplicated here.",
  "scripts": {
    "test": "node --test \"development/tests/**/*.test.js\"",
    "sync-upstream": "node development/sync-upstream.js",
    "build-slices": "node development/build-rule-slices.js"
  }
}
